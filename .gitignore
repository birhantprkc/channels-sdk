# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Skills fetched from the CopilotKit registry, not owned here (see AGENTS.md).
# `skills install` writes into these paths; committing what it writes is how the
# vendored setup-slack-channel copy drifted ~16 KB behind its single home.
/.agents/skills/setup-slack-channel/
/agent/
/skills-lock.json
